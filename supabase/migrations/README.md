# Database migrations

Add timestamped Supabase SQL migrations here (for example, `20260901090000_create_profiles.sql`). Once deployed to a shared environment, migrations are append-only: correct schema with a new migration rather than rewriting history. Phase 0 deliberately creates no business tables.
